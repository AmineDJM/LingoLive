# Expo / Metro do not follow pnpm's symlinked node_modules reliably.
# Hoisted linking is the officially documented pnpm setup for Expo monorepos.
node-linker=hoisted
# Keep peer resolution strict enough to surface real incompatibilities.
strict-peer-dependencies=false
auto-install-peers=true
# Reproducible CI installs.
resolution-mode=highest
